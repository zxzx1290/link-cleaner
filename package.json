{
  "name": "link-cleaner",
  "version": "0.0.0",
  "description": "清除連結追蹤參數並還原包裝網址的 PWA（Vue + Cloudflare Worker）",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler dev",
    "deploy": "vite build && wrangler deploy",
    "test": "node --test",
    "icons": "node tools/make-icons.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "vite": "^8.2.0",
    "wrangler": "^4.0.0"
  }
}
