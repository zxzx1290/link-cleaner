name = "link-cleaner"
main = "worker/index.js"
compatibility_date = "2026-07-01"

# vite build 的產出；靜態檔案由 Cloudflare 邊緣直接回應，不計入 Worker 請求
[assets]
directory = "./dist"
binding = "ASSETS"
# /api/* 一定要先進 Worker，其餘路徑優先當靜態資產處理
run_worker_first = ["/api/*"]

[observability]
enabled = true
